// Mukesh
// Todo:
// convert the text into binary based on recieved data
// left -0 and right -1
int compress(FILE *input, FILE *output, long freq[256]){
    if(!output){
        printf("Error: Unable to create the compress file\n");
        return 1;
    }
    
    return 0;
}